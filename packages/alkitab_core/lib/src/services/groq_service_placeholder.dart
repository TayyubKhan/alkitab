// Ideally this should be in alkitab_core or abstracted.
// For now, I'll assume it's in core or stub it if it was local.
// Wait, ResearchViewModel imported remote/groq_service.dart.
// I need to check where `groq_service.dart` is.
// It was `import '../../../../data/remote/groq_service.dart';`.
// I should move GroqService to Core as well.
